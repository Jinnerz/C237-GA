<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/homepagestyles.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Account information</title>
    <style>
        #accountlink {
            background-color: darkgreen;
        }
    </style>
</head>
<body style="background-color: black;">
    <!-- user id passed into navbar for proper navigation -->
    <%- include ('partials/navbar.ejs', {userId: userId}) %>
    <div class="container mt-3" style="background-color: gray;">
        <h2 style="text-align: center; color: white; text-decoration: underline;">Your Account Information</h2>

        <div class="container-fluid p-5 mb-5 text-white">
            <div class="row">
                <!-- 1st column (profile photo)-->
                <div class="col-sm-4" style="padding: 10px;">
                    <img src="/images/<%= user.image %>" width="80%"><br>
                </div>

                <!-- 2nd column (firstName, LastName, Email)-->
                <div class="col-sm-4 d-flex flex-column">

                    <div class="mb-3 mt-3">
                        <label for="firstName">First Name:</label>
                        <%= user.firstName %>
                    </div>

                    <div class="mb-3 mt-3">
                        <label for="lastName">Last Name:</label>
                        <%= user.lastName %>
                    </div>

                    <div class="mb-3 mt-3">
                        <label for="email">Email:</label>
                        <%= user.email %>
                    </div>

                </div>

                <!-- 3rd column (birthday, contact, age)-->
                <div class="col-sm-4 d-flex flex-column">

                    <div class="mb-3 mt-3">
                        <label for="birthday">Birthday:</label>
                        <%= user.birthday.toLocaleDateString('en-US', {year:'numeric', month:'short',day:'2-digit'}) %>
                    </div>

                    <div class="mb-3 mt-3">
                        <label for="contact">Contact:</label>
                        <%= user.contact %>
                    </div>

                    <div class="mb-3 mt-3">
                        <label for="age">Age:</label>
                        <%= user.age %>
                    </div>
                    
                </div>
                <a href="/user/<%= userId %>/editAccount" class="btn btn-primary">Edit Account Information</a>
            </div>
        </div>

    </div>

</body>
</html>