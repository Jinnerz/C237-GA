<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Account information</title>
</head>
<body style="background-color: black; color: white;">
    <!-- user id passed into navbar for proper navigation -->
    <%- include ('partials/navbar.ejs', {userId: userId}) %>

    <h2 style="align-self: center; text-align: center;">Update Account</h2>

    <div class="container mt-3" style="border: 1px white solid; display: flex; justify-content: center;">

            <% if (user) { %>
                <form action="/user/<%= userId %>/editAccount" method="POST" enctype="multipart/form-data">
                    <label for="firstName">First Name:</label><br>
                    <input style="background-color: gray;" type="text" id="firstName" name="firstName" value= <%= user.firstName %> readonly><br>
        
                    <label for="lastName">Last Name:</label><br>
                    <input style="background-color: gray;" type="text" id="lastName" name="lastName" value= <%= user.lastName %> readonly><br>

                    <label for="email">Email:</label><br>
                    <input type="text" id="email" name="email" value= <%= user.email %>><br>
        
                    <label for="birthday">Birthday:</label><br>
                    <!-- What is "readonly"? An attribute in HTML that makes them non-editable -->
                    <!-- Why type="text" instead of type="date" for birthday? user-friendly format, -->
                    <input style="background-color: gray;" type="text" id="birthday" name="birthday" value="<%= user.birthday.toLocaleDateString('en-US', {year:'numeric', month:'short',day:'2-digit'})%>" readonly><br>
                    
                    <label for="contact">Contact:</label><br>
                    <input type="text" id="contact" name="contact" value= <%= user.contact %>><br>

                    <label for="age">Age:</label><br>
                    <input type="number" id="age" name="age" value= <%= user.age %>><br>
        
                    <label for="image">Current Image:</label><br>
                    <input style="background-color: gray;" type="text" name="currentImage" value= <%= user.image %> readonly><br><br>
                    <img src= "/images/<%= user.image %>" width="20%"><br>

                    New Image: <br>
                    <input type="file" id="image" name="image" accept="image/*" value= <% user.image %>><br><br>
        
                    <button type="submit" class="btn btn-primary">Update Account</button>
                    <a href="/user/<%= userId %>">Back</a>
                </form>
            <% } else { %>
                <p>No User found.</p>
            <% } %>

    </div>
</body>
</html>